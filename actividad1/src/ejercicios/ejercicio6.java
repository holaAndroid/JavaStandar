package ejercicios;

import java.util.Scanner;

public class ejercicio6 {

	public static void main(String[] args) {
		// Realiza un programa que, dado el peso y la altura de una persona,
		// calcule el índice de masa corporal. El IMC es peso/altura*altura.
		/*
		 * Los valores de IMC se clasifican de la siguiente manera:

    IMC < 18.5: Peso insuficiente
    18.5 ≤ IMC < 25: Peso saludable
    25 ≤ IMC < 30: Sobrepeso
    30 ≤ IMC < 35: Obesidad grado I
    35 ≤ IMC < 40: Obesidad grado II
    IMC ≥ 40: Obesidad grado III 
    */
/*	double peso, altura, imc;
		Scanner entrada = new Scanner(System.in);
		System.out.println("Introduce el peso en kilogramos; ");
		peso = entrada.nextDouble();
		System.out.println("Ingrese la altura en metros: ");
		altura = entrada.nextDouble();
		if (altura !=0) { //Evitar siempre las divisiones por 0
				imc= Match.round(peso /(altura*altura));
		}
		double imc = peso /(altura*altura);
		System.out.println("Tu Indice de Masa Corporal (IMC) es " + imc);*/
	}
}
		
